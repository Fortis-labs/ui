/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { FORTIS_MULTISIG_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const PROPOSAL_EXECUTE_DISCRIMINATOR = new Uint8Array([3]);

export function getProposalExecuteDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 1).encode(
    PROPOSAL_EXECUTE_DISCRIMINATOR
  );
}

export type ProposalExecuteInstruction<
  TProgram extends string = typeof FORTIS_MULTISIG_PROGRAM_ADDRESS,
  TAccountMultisig extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TAccountTransaction extends string | AccountMeta<string> = string,
  TAccountMember extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMultisig extends string
      ? ReadonlyAccount<TAccountMultisig>
      : TAccountMultisig,
      TAccountProposal extends string
      ? WritableAccount<TAccountProposal>
      : TAccountProposal,
      TAccountTransaction extends string
      ? ReadonlyAccount<TAccountTransaction>
      : TAccountTransaction,
      TAccountMember extends string
      ? ReadonlySignerAccount<TAccountMember> &
      AccountSignerMeta<TAccountMember>
      : TAccountMember,
      ...TRemainingAccounts,
    ]
  >;

export type ProposalExecuteInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ProposalExecuteInstructionDataArgs = {};

export function getProposalExecuteInstructionDataEncoder(): FixedSizeEncoder<ProposalExecuteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 1)]]),
    (value) => ({ ...value, discriminator: PROPOSAL_EXECUTE_DISCRIMINATOR })
  );
}

export function getProposalExecuteInstructionDataDecoder(): FixedSizeDecoder<ProposalExecuteInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 1)],
  ]);
}

export function getProposalExecuteInstructionDataCodec(): FixedSizeCodec<
  ProposalExecuteInstructionDataArgs,
  ProposalExecuteInstructionData
> {
  return combineCodec(
    getProposalExecuteInstructionDataEncoder(),
    getProposalExecuteInstructionDataDecoder()
  );
}

export type ProposalExecuteInput<
  TAccountMultisig extends string = string,
  TAccountProposal extends string = string,
  TAccountTransaction extends string = string,
  TAccountMember extends string = string,
> = {
  multisig: Address<TAccountMultisig>;
  /** The proposal account associated with the transaction. */
  proposal: Address<TAccountProposal>;
  /** The transaction to execute. */
  transaction: Address<TAccountTransaction>;
  member: Address<TAccountMember>;
};

export function getProposalExecuteInstruction<
  TAccountMultisig extends string,
  TAccountProposal extends string,
  TAccountTransaction extends string,
  TAccountMember extends string,
  TProgramAddress extends Address = typeof FORTIS_MULTISIG_PROGRAM_ADDRESS,
>(
  input: ProposalExecuteInput<
    TAccountMultisig,
    TAccountProposal,
    TAccountTransaction,
    TAccountMember
  >,
  config?: { programAddress?: TProgramAddress }
): ProposalExecuteInstruction<
  TProgramAddress,
  TAccountMultisig,
  TAccountProposal,
  TAccountTransaction,
  TAccountMember
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FORTIS_MULTISIG_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    multisig: { value: input.multisig ?? null, isWritable: false },
    proposal: { value: input.proposal ?? null, isWritable: true },
    transaction: { value: input.transaction ?? null, isWritable: false },
    member: { value: input.member ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.multisig),
      getAccountMeta(accounts.proposal),
      getAccountMeta(accounts.transaction),
      getAccountMeta(accounts.member),
    ],
    data: getProposalExecuteInstructionDataEncoder().encode({}),
    programAddress,
  } as ProposalExecuteInstruction<
    TProgramAddress,
    TAccountMultisig,
    TAccountProposal,
    TAccountTransaction,
    TAccountMember
  >);
}

export type ParsedProposalExecuteInstruction<
  TProgram extends string = typeof FORTIS_MULTISIG_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    multisig: TAccountMetas[0];
    /** The proposal account associated with the transaction. */
    proposal: TAccountMetas[1];
    /** The transaction to execute. */
    transaction: TAccountMetas[2];
    member: TAccountMetas[3];
  };
  data: ProposalExecuteInstructionData;
};

export function parseProposalExecuteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedProposalExecuteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      multisig: getNextAccount(),
      proposal: getNextAccount(),
      transaction: getNextAccount(),
      member: getNextAccount(),
    },
    data: getProposalExecuteInstructionDataDecoder().decode(instruction.data),
  };
}
